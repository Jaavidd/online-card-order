spring:
  application:
    name: #confidential
  cloud:
    consul:
      config:
        enabled: true
      host: consul.service.consul
      port: 8500
      enabled: true
    vault:
      host: vault.service.consul
      port: 8200
      scheme: http
      authentication: token
      token: #  confidential
      enabled: true
